name: pop_back_handler
description: A Flutter widget that provides unified handling for back navigation events, integrating PopScope with iOS swipe-back gesture detection.
version: 1.0.0
homepage: https://github.com/user/pop_back_handler
repository: https://github.com/user/pop_back_handler
issue_tracker: https://github.com/user/pop_back_handler/issues

environment:
  sdk: ^3.0.0
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
